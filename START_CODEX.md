# START CODEX

Abra este diretório como repositório no Codex e envie:

> Execute o loop atual do Portal FUMEP. Leia primeiro `AGENTS.md` e `docs/CURRENT_LOOP.md`. Use a skill `execute-loop`. Trabalhe somente no loop atual, execute os checks aplicáveis, atualize os arquivos de status e faça commit ao concluir.

O estado inicial deste pacote é:

```text
Loop 0.1 concluído
Loop 0.2 atual
```
